# API Design

## Server Actions
- Auth: `register`, `login`, `assignRole`.
- Tables: `getTableConfig`, `updateTableConfig`, `blockDates`.
- Reservations: `getAvailability`, `createReservation`, `updateReservation`, `cancelReservation`.
- Chat: `sendMessage`, `getMessages`.
- Notifications: `getSystemNotifications`.
